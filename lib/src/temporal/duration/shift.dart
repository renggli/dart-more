import '../time_unit.dart';

extension ShiftToDurationExtension on Duration {
  /// Converts the [Duration] into a representation in a different set of units.
  Map<TimeUnit, num> shiftTo(Set<TimeUnit> units) => throw UnimplementedError();
}
